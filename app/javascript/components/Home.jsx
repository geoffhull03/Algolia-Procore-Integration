import React from "react"
import PropTypes from "prop-types"

class Home extends React.Component {
  render () {
    return (
        <div>
          <p>React-Rails is setup and ready to go</p>
        </div>
    );
  }
}

export default Home
